onclick  点击
alert  弹出
获取元素的方法
1.document.getElementById();  文档 找到 元素 输出 id
2.document.getElementByClassName();
2.document.getElementByTagName();

获取div p ul ol li dl dt dd文本值
1.用innerHTML
2.用innerText
赋值
2.用innerText=''
1.用innerHTML=''
var box=docment.getElementById('');
console.log(box.innerHTML);
获取表单的值

function 函数

display:block  行内变块
display:inline  块变行内

输出的三种方式
1.alert() 弹出
2.console.log()
3.prompt()提示况

setInterval  定时器

lis[i].index = i;
console.log(this.index);


parseInt  强制转换成整数


-webkit-animation: 名字 时间 速度曲线；
匀速
又快到慢 liner
又慢开始 ease
又慢结束 ease-out
又慢开始又慢结束  ease-in-out


值	描述
animation-name	规定需要绑定到选择器的 keyframe 名称。。
animation-duration	规定完成动画所花费的时间，以秒或毫秒计。
animation-timing-function	规定动画的速度曲线。
animation-delay	规定在动画开始之前的延迟。
animation-iteration-count	规定动画应该播放的次数。
animation-direction	规定是否应该轮流反向播放动画。

n次数值 infinite无限 播放次数
normal alternate 动画的方向倒序



-webkit-transform: 旋转 rotate3的（x y z）
                   倾斜 skew
                   缩放
                   平移



       display:none  不占位的
       选择器
       +  谁知后的元素


       animation transition
      1. animation 不需要时间触发
       transition 需要时间触发